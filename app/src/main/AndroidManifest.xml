<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinalProject"
        tools:targetApi="31">

        <activity
            android:name="trivia.ui.TriviaQuestionApp"
            android:exported="false" >
        </activity>
        <activity
            android:name="trivia.ui.QuestionsAttempt"
            android:exported="false" />
        <activity
            android:name="trivia.ui.Result"
            android:exported="false" />
        <activity
            android:name="trivia.ui.Results"
            android:exported="false" />
        <activity
            android:name=".CurrencyConvert.CurrencyActivity">
        </activity>
        <activity
            android:name=".CurrencyConvert.SavedConversionsActivity">
        </activity>
        <activity android:name=".AviationActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>